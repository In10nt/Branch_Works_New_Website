// API Configuration
// For production, use HTTPS domain
// For development, use HTTP localhost

const API_BASE_URL = process.env.REACT_APP_API_URL || 'https://api.branchworksglobal.com';

export default API_BASE_URL;
