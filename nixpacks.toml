[phases.setup]
nixPkgs = ['nodejs-18_x', 'maven', 'jdk17']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = [
  'npm run build',
  'cd backend && mvn clean package -DskipTests'
]

[start]
cmd = 'cd backend && java -Dserver.port=$PORT -jar target/coming-soon-backend-0.0.1-SNAPSHOT.jar'
