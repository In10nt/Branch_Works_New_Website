version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  
  build:
    commands:
      - echo Building Spring Boot backend...
      - cd backend
      - mvn clean package -DskipTests
      - cd ..

artifacts:
  files:
    - backend/target/*.jar
  discard-paths: yes
